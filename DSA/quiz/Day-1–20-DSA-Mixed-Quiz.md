Day 1–20 DSA Mixed Quiz — 20 MCQs

1. Which data structure is best for checking duplicates quickly?

A. List  
B. Set  
C. Stack  
D. Queue  

2. In Two Sum, what do we usually store in hashmap?

A. Only target  
B. Number and its index  
C. Only indexes  
D. Sorted values  

3. An anagram requires:

A. Same length only  
B. Same characters with same frequency  
C. Same first and last character  
D. Sorted original strings only  

4. Valid Palindrome commonly uses:

A. Stack only  
B. Two pointers  
C. Queue only  
D. Binary search  

5. Two Sum II works efficiently because the array is:

A. Unsorted  
B. Sorted  
C. Empty  
D. Circular  

6. Fixed sliding window is useful when:

A. Window size is fixed  
B. Window size changes randomly  
C. Data must be sorted  
D. Recursion is required  

7. Variable sliding window is useful when:

A. We always need exactly `k` elements  
B. Window expands/shrinks based on condition  
C. We only use recursion  
D. We need linked list reversal  

8. Prefix sum helps answer range sum queries in:

A. `O(n)` after preprocessing  
B. `O(1)` after preprocessing  
C. `O(n²)`  
D. `O(log n)` always  

9. Subarray Sum Equals K commonly uses:

A. Prefix sum + hashmap  
B. Stack only  
C. Queue only  
D. Binary tree  

10. Valid Parentheses uses:

A. Stack  
B. Queue  
C. Two pointers  
D. Binary search  

11. Min Stack needs extra tracking so `get_min()` can be:

A. `O(n)`  
B. `O(log n)`  
C. `O(1)`  
D. `O(n²)`  

12. Daily Temperatures uses which pattern?

A. Monotonic stack  
B. Prefix sum only  
C. Binary search only  
D. Recursion only  

13. RecentCounter should remove calls older than:

A. `1000 ms`  
B. `2000 ms`  
C. `3000 ms`  
D. `5000 ms`  

14. To reverse a linked list iteratively, we mainly track:

A. `previous`, `current`, `next_node`  
B. `left`, `right`, `mid`  
C. `stack`, `min_stack`  
D. `prefix`, `count`  

15. Search Insert Position uses:

A. Linear search only  
B. Binary search  
C. Stack  
D. Queue  

16. Koko Eating Bananas searches:

A. Banana pile indexes  
B. Possible eating speed values  
C. Sorted banana piles  
D. Number of monkeys  

17. Ship Packages Within D Days searches:

A. Package indexes  
B. Possible capacity values  
C. Sorted weights  
D. Queue size  

18. Factorial recursion needs:

A. Base case and recursive case  
B. Hashmap and queue  
C. Two pointers only  
D. Binary search only  

19. Naive Fibonacci is slow because:

A. It repeats subproblems  
B. It sorts values  
C. It uses no recursion  
D. It uses a queue  

20. Recursive array sum should stop when:

A. `index == 0`  
B. `index == len(nums)`  
C. `nums[index] == 0`  
D. `index == nums[index]`  

Send answers like:

```text
1.B
2.B
3.B
...
20.B
```
