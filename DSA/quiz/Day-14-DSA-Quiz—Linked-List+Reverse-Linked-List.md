## Day 14 DSA Quiz — Linked List + Reverse Linked List

Reply like:

```text
1.A
2.B
3.C
...
15.A
```

### 1. A linked list is made of:

A. Nodes connected by pointers  
B. Only indexes  
C. SQL rows  
D. Sorted strings

### 2. Each linked list node usually stores:

A. value and next pointer  
B. only index  
C. only previous total  
D. only stack top

### 3. Last node points to:

A. First node  
B. None  
C. Middle node  
D. Random node

### 4. 80/20 core of linked list problems is:

A. Always sort nodes  
B. Pointer rewiring  
C. Use binary search  
D. Use SQL joins

### 5. Reverse linked list means:

A. Reverse links  
B. Sort values  
C. Delete nodes  
D. Count duplicate values

### 6. For `1 → 2 → 3 → None`, reversed list is:

A. `3 → 2 → 1 → None`  
B. `1 → 2 → 3 → None`  
C. `2 → 1 → 3 → None`  
D. `None → 1 → 2 → 3`

### 7. In reverse list, `previous` means:

A. Reversed part built so far  
B. Future node only  
C. Last value only  
D. Stack top

### 8. `current` means:

A. Node being changed now  
B. Always first node  
C. Always last node  
D. Previous node only

### 9. Why save `next_node` before changing `current.next`?

A. To avoid losing the remaining list  
B. To sort the list  
C. To count nodes  
D. To use more memory

### 10. Correct pointer update order is:

A. Save next → reverse current.next → move previous → move current  
B. Reverse current.next → save next → move current → move previous  
C. Move current → reverse next → save previous  
D. Sort → reverse → return

### 11. After reversing the full list, what should return?

A. Old head  
B. previous  
C. current  
D. next_node

### 12. Reverse linked list time complexity is:

A. O(1)  
B. O(n)  
C. O(n²)  
D. O(log n)

### 13. Reverse linked list space complexity is:

A. O(1)  
B. O(n)  
C. O(n²)  
D. O(log n)

### 14. Which is a common mistake?

A. Saving `next_node`  
B. Returning old head  
C. Moving current forward  
D. Reversing pointer

### 15. Key rule for reverse linked list is:

A. Save next, reverse pointer, move forward  
B. Sort first, then reverse  
C. Use hash map always  
D. Use queue only
