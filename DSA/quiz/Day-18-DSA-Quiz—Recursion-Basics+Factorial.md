Day 18 DSA Quiz — Recursion Basics + Factorial

1. What is recursion?

A. A loop that runs forever  
B. A function calling itself to solve a smaller problem  
C. Sorting an array repeatedly  
D. Searching only sorted arrays  

2. Every recursive function must have:

A. Base case and recursive case  
B. Only a loop  
C. Only one return statement  
D. A dictionary  

3. What is the base case?

A. The condition where recursion stops  
B. The first line of every function  
C. The largest input  
D. The loop condition  

4. What is the recursive case?

A. The part where the function calls itself with a smaller problem  
B. The part where we sort data  
C. The part where we create a list  
D. The part where we print output only  

5. What happens if there is no base case?

A. Function returns `0` automatically  
B. Recursion stops after one call  
C. Recursion keeps calling until stack overflow / recursion error  
D. Python converts it into a loop  

6. What is `5!`?

A. `5 + 4 + 3 + 2 + 1`  
B. `5 * 4 * 3 * 2 * 1`  
C. `5 * 5`  
D. `5 - 4 - 3 - 2 - 1`  

7. What is the value of `5!`?

A. `24`  
B. `120`  
C. `25`  
D. `15`  

8. What is `0!`?

A. `0`  
B. `1`  
C. Undefined  
D. `None`  

9. Correct recursive formula for factorial is:

A. `factorial(n) = n + factorial(n - 1)`  
B. `factorial(n) = n * factorial(n - 1)`  
C. `factorial(n) = factorial(n + 1)`  
D. `factorial(n) = n * n`  

10. In factorial recursion, which input moves toward the base case?

A. `n + 1`  
B. `n - 1`  
C. `n * 2`  
D. `n`  

11. In Python, what happens if recursive calls go too deep?

A. `RecursionError`  
B. `SyntaxError`  
C. `KeyError`  
D. `TypeError`  

12. Why is recursion space complexity often `O(n)`?

A. Because it sorts the array  
B. Because each recursive call waits on the call stack  
C. Because it always creates a hashmap  
D. Because Python uses binary search  

13. Time complexity of recursive factorial is:

A. `O(1)`  
B. `O(log n)`  
C. `O(n)`  
D. `O(n²)`  

14. Space complexity of recursive factorial is:

A. `O(1)`  
B. `O(n)`  
C. `O(log n)`  
D. `O(n²)`  

15. Which is a common recursion mistake?

A. Having a base case  
B. Moving toward the base case  
C. Forgetting `return`  
D. Calling with smaller input  

Send answers like:

```text
1.B
2.A
3.A
...
15.C
```
