# Python Day 0 Quiz

Try without looking at the theory. Write the answers separately.

## Part A — Multiple choice

### 1. What is programming?

A. Giving a computer clear instructions  
B. Repairing computer hardware  
C. Using the internet  
D. Installing applications  

### 2. What is an algorithm?

A. A Python error  
B. A step-by-step solution to a problem  
C. A programming language  
D. A computer component  

### 3. What does the Python interpreter do?

A. Designs websites automatically  
B. Reads and executes Python code  
C. Repairs logic errors automatically  
D. Stores images  

### 4. Which file is probably a Python program?

A. `program.jpg`  
B. `program.pdf`  
C. `program.py`  
D. `program.mp3`  

### 5. Where do we normally write code?

A. Calculator  
B. Editor or IDE  
C. Camera  
D. File explorer  

### 6. What normally displays program output and errors?

A. Keyboard  
B. Terminal or console  
C. Printer  
D. Browser history  

### 7. Which order represents the basic program model?

A. Output → Input → Process  
B. Process → Output → Input  
C. Input → Process → Output  
D. Input → Output → Process  

### 8. In a shop-bill program, which is the process?

A. Reading the item name  
B. Multiplying price by quantity  
C. Printing the total  
D. Opening the editor  

### 9. How does Python normally read statements?

A. Bottom to top  
B. Randomly  
C. Top to bottom  
D. Right to left  

### 10. What does this statement do?

```python
name = "Veena D"
```

A. Prints the name  
B. Stores the name  
C. Deletes the name  
D. Requests user input  

### 11. What does this statement do?

```python
print(name)
```

A. Stores `name`  
B. Changes `name`  
C. Displays the value stored in `name`  
D. Deletes `name`  

### 12. Which code is correct?

A.

```python
Print("Hello")
```

B.

```python
print("Hello")
```

C.

```python
print(Hello)
```

D.

```python
print "Hello"
```

### 13. What type of error is present?

```python
print("Welcome"
```

A. Logic error  
B. Runtime error  
C. Syntax error  
D. No error  

### 14. What type of error may occur here?

```python
result = 10 / 0
```

A. Runtime error  
B. Syntax error  
C. Logic error  
D. Spelling error  

### 15. The program runs, but its answer is incorrect. What type of error is this?

A. Syntax error  
B. Runtime error  
C. Logic error  
D. File error  

---

## Part B — Predict the output

### 16. What is the output?

```python
print("Started")
print("Learning Python")
print("Completed")
```

### 17. What is the output?

```python
price = 50
quantity = 3
total = price * quantity

print(total)
```

### 18. Number these statements in their execution order:

```python
print("Area =", area)
width = 4
area = length * width
length = 10
```

The statements are intentionally mixed. Rewrite them in the correct order.

---

## Part C — Identify the program stages

### 19. Rectangle-area problem

```text
Length = 8
Width = 5
Area = length × width
Print the area
```

Identify:

```text
Input:
Process:
Output:
```

### 20. Student-total problem

A student has marks of `80`, `90`, and `70`.

Identify:

```text
Input:
Process:
Output:
```

---

## Part D — Errors and corrections

### 21. Find and correct the error:

```python
Print("Welcome to Python")
```

### 22. Find and correct the error:

```python
name = Veena D
```

### 23. Find and correct the error:

```python
length = 10
width = 4
area = length + width

print("Area =", area)
```

Also identify the error type.

### 24. Why does this fail?

```python
print(city)
city = "Dharmavaram"
```

Rewrite it correctly.

---

## Part E — Algorithm thinking

### 25. Write an algorithm to calculate the final bill

The program receives:

```text
Price of one item
Quantity purchased
```

It must calculate and display the final bill.

Write the steps without Python code.

### 26. Write an algorithm to calculate the average of three marks.

### 27. Write an algorithm to convert kilometres into metres.

Use:

```text
metres = kilometres × 1000
```

---

## Part F — Feynman teach-back

Explain these aloud using simple words:

### 28. What is the difference between an algorithm and a program?

### 29. Explain what happens between writing Python code and seeing its output.

### 30. Explain the difference between:

- Syntax error
- Runtime error
- Logic error

## Scoring

```text
27–30: Excellent Day 0 understanding
23–26: Good; review incorrect answers
18–22: Review algorithms, execution and errors
Below 18: Repeat Day 0 using simpler examples
```

Day 0 is complete only when the answers can be **explained**, not merely selected.
